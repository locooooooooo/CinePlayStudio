# CinePlayStudio 发布证据模板

> 本模板用于证明某一版本卡或单项发布门禁的真实结果。复制为独立记录后填写；不得在模板本身登记通过。

## 1. 记录信息

- 对应版本：
- 对应门禁：
- 结论：`passed | failed | blocked | not_run`
- 源码 commit：
- 工作区状态：`clean | dirty`；若为 dirty，附变更清单和影响判断
- 执行日期：
- 实现负责人：
- 验收负责人：

## 2. 环境

- Windows 版本与 build：
- CPU / RAM / GPU / 磁盘：
- 显示分辨率与 DPI：
- Node / npm 版本：
- 安装方式：`dev | unpacked | installer`
- 网络及权限限制：

## 3. 自动化门禁

| 命令或检查             | 结果      | 用时 | 日志或产物位置 |
| ---------------------- | --------- | ---- | -------------- |
| `npm ci`               | `not_run` |      |                |
| `npm run typecheck`    | `not_run` |      |                |
| `npm run lint`         | `not_run` |      |                |
| `npm run format:check` | `not_run` |      |                |
| `npm run test`         | `not_run` |      |                |
| `npm run build`        | `not_run` |      |                |

版本卡要求的 installer、Electron E2E、security、performance 或 packaged smoke 在此表继续追加，不得省略。

## 4. 用户场景

| 场景 | 前置数据 | 预期结果 | 实际结果 | 证据 | 结论      |
| ---- | -------- | -------- | -------- | ---- | --------- |
|      |          |          |          |      | `not_run` |

场景失败时记录稳定错误码、是否损伤原项目、恢复步骤和复测结果。不能只附成功截图。

## 5. 发行产物

| 产物 | 版本 | 文件大小 | SHA256 | 签名主体与时间戳 | 来源 commit |
| ---- | ---- | -------- | ------ | ---------------- | ----------- |
|      |      |          |        |                  |             |

## 6. 外部验证

- 目标用户画像：
- 招募人数 / 有效人数：
- 独立完成核心旅程人数：
- 完成率：
- 使用的真实素材类型：
- 主要阻塞与误解：
- 诊断信息同意方式：

没有进入外部验证阶段时明确填写 `not_applicable`，不能留空。

## 7. 已知问题与残余风险

| 问题或风险 | 严重度 | 用户影响 | 临时措施 | 责任人 | 关闭版本 |
| ---------- | ------ | -------- | -------- | ------ | -------- |
|            |        |          |          |        |          |

## 8. 晋级结论

- 版本卡硬验收是否全部通过：
- 是否存在 P0 / P1：
- 是否违反 1.0 非目标或安全边界：
- 决策：`promote | hold | rollback`
- 决策依据：
- 验收签字与日期：
